# Конфигурация Wi-Fi сканера для OpenWrt
# Скопируйте этот файл в /etc/scanner.conf на роутере и настройте параметры

# Интерфейс для мониторинга (обычно mon0)
INTERFACE="mon0"

# IP адрес Windows PC с MQTT брокером
# ВАЖНО: Замените на реальный IP адрес вашего Windows PC
# IP роутера: 192.168.1.100
# IP Windows PC: 192.168.1.101
MQTT_HOST="192.168.1.101"

# MQTT топик для отправки данных
MQTT_TOPIC="wifi/probes"

# Интервал отправки в секундах. 600 = каждые 10 минут (график с шагом по 10 мин)
CYCLE_TIME=600

# ---- Настройки стабильности сети ----

# Рабочий канал вашей Wi-Fi сети (автоопределение если пусто)
# Укажите явно если автоопределение не работает (обычно 1, 6 или 11)
#HOME_CHANNEL="6"

# Время ожидания восстановления сети после channel hopping (сек)
# Увеличьте если сеть долго восстанавливается (по умолчанию 15)
NETWORK_SETTLE_TIME=15

# Макс. время ожидания доступности MQTT хоста (сек, по умолчанию 120)
HOST_WAIT_TIMEOUT=120

# Кол-во попыток отправки MQTT (по умолчанию 5)
MQTT_RETRIES=5

# Начальная задержка между попытками (сек, удваивается, макс 30с)
MQTT_RETRY_DELAY=3

# Макс. кол-во буферизованных сообщений (по умолчанию 10)
# Если MQTT недоступен, данные сохраняются и досылаются при следующем цикле
BUFFER_MAX=10
